{
  "name": "pg-game",
  "version": "1.0.0",
  "description": "Procgen game",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify build.js -o bundle.js -t babelify",
    "watch": "watchify build.js -o bundle.js -t babelify"
  },
  "author": "",
  "license": "ISC",
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "devDependencies": {
    "babel": "^6.23.0",
    "babel-core": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^8.0.0"
  }
}
